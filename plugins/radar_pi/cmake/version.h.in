#define PLUGIN_VERSION_MAJOR ${VERSION_MAJOR}
#define PLUGIN_VERSION_MINOR ${VERSION_MINOR}
#define PLUGIN_VERSION_PATCH "${VERSION_PATCH}"
#define PLUGIN_VERSION_DATE "${VERSION_DATE}"
#define PLUGIN_VERSION "radar_pi v${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_PATCH}-${OCPN_MIN_VERSION}"
#define PLUGIN_VERSION_WITH_DATE "radar_pi v${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_PATCH}-${OCPN_MIN_VERSION} released on ${VERSION_DATE}"
